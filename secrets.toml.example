# Optional: copy to .streamlit/secrets.toml (local) or paste into
# Streamlit Cloud → App settings → Secrets.
#
# Pre-fill an API key so you don't paste it every time. The app also lets you
# type a key directly in the sidebar, so secrets are optional.

gemini_api_key = ""
groq_api_key = ""
deepseek_api_key = ""
openai_api_key = ""
anthropic_api_key = ""
